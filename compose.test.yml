name: fcp-sfd-comms-publisher-stub-test

services:
  fcp-sfd-comms-publisher-stub:
    build:
      target: development
    command: npm test
    volumes:
      - ./test:/home/node/test
      - ./coverage:/home/node/coverage
